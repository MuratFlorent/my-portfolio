---
import { getCollection } from 'astro:content';

// Layout import — provides basic page elements: <head>, <nav>, <footer> etc.
import BaseLayout from '../layouts/BaseLayout.astro';

// Component Imports
import CallToAction from '../components/CallToAction.astro';
import Grid from '../components/Grid.astro';
import Hero from '../components/Hero.astro';
import Icon from '../components/Icon.astro';
import Pill from '../components/Pill.astro';
import PortfolioPreview from '../components/PortfolioPreview.astro';
import ProjectModal from '../components/ProjectModal.astro';
import UniverseModal from '../components/UniverseModal.astro';
import ScrollSpy from '../components/ScrollSpy.astro';
import Testimonials from '../components/Testimonials.astro';

// Page section components
import ContactCTA from '../components/ContactCTA.astro';
import Skills from '../components/Skills.astro';

// Content Fetching: List four most recent work projects
const projects = (await getCollection('work'))
	.sort((a, b) => b.data.publishDate.valueOf() - a.data.publishDate.valueOf())
	.slice(0, 4);

// Full Astro Component Syntax:
// https://docs.astro.build/core-concepts/astro-components/
---

<BaseLayout>
	<ProjectModal id="project-modal" />
	<UniverseModal id="universe-modal" />
	<ScrollSpy />
	<div class="stack gap-12 lg:gap-16">
		<div class="wrapper stack gap-8 lg:gap-10">
			<header id="accueil" class="hero">
				<Hero
					title="Sites web sur mesure et visibilité sur Google"
					tagline="Je conçois des solutions digitales adaptées à votre projet pour améliorer votre présence en ligne et attirer les bons clients."
					align="start"
				>
					<div class="roles">
						<Pill href="/developpeur-web-freelance" ariaLabel="Découvrir mon approche">
							<Icon icon="code" size="1.33em" /> Développeur web
						</Pill>
						<Pill href="/formation" ariaLabel="Voir la section formation">
							<Icon icon="graduation-cap" size="1.33em" /> Formateur
						</Pill>
						<Pill href="#contact" ariaLabel="Parler de votre projet" id="open-quote-pill">
							<Icon icon="users" size="1.33em" /> Accompagnement sur mesure
						</Pill>
						<Pill href="tel:+33682027290" ariaLabel="Appeler Florent Murat">
							<Icon icon="phone" size="1.33em" /> Rendez-vous téléphonique
						</Pill>
					</div>

					<div class="hero-ctas">
						<CallToAction href="/work/">
							Mes projets
							<Icon icon="arrow-right" size="1.2em" />
						</CallToAction>
						<CallToAction href="mailto:murat.light.developer@gmail.com">
							Discutons de votre projet
							<Icon icon="paper-plane-tilt" size="1.2em" />
						</CallToAction>
					</div>
				</Hero>

					<div class="hero-image-container">
					<img
						alt="Florent Murat - Développeur web freelance spécialisé en création de site sur mesure et SEO"
						width="1365"
						height="2048"
						src="/assets/portrait2.jpeg"
						loading="eager"
						decoding="async"
					/>
					<div class="image-overlay">
						<p class="overlay-title">Florent Murat — Light Developer</p>
						<p class="overlay-text">Développeur web, formateur et accompagnant de projets sur mesure.</p>
					</div>
				</div>

			</header>

			<section id="solutions" class="section solutions">
				<div class="solutions-bg"></div>
				<header class="section-header stack gap-2 lg:gap-4">
					<h2>Solutions personnalisées</h2>
					<p>Que vous ayez besoin d'une présence en ligne complète ou d'un outil métier spécifique, je conçois des solutions adaptées à vos enjeux.</p>
				</header>

				<div class="gallery">
					<Grid variant="small">
						<li class="service-card">
							<a href="/creation-site-web-sur-mesure" class="service-link">
								<Icon icon="code" color="var(--accent-regular)" size="2.5rem" gradient />
								<h4>Sites & refontes</h4>
								<p>Création de sites vitrines et applicatifs modernes, optimisés pour votre activité.</p>
							</a>
						</li>
						<li class="service-card">
							<Icon icon="cpu" color="var(--accent-regular)" size="2.5rem" gradient />
							<h4>Fonctionnalités sur mesure</h4>
							<p>Extension des capacités de vos outils existants (WordPress, Sylius, etc.) via des plugins spécifiques.</p>
						</li>
						<li class="service-card">
							<a href="/automatisation-web" class="service-link">
								<Icon icon="magic-wand" color="var(--accent-regular)" size="2.5rem" gradient />
								<h4>Automatisations & gain de temps</h4>
								<p>Optimisation de vos processus métier via des scripts et outils connectés pour gagner en productivité.</p>
							</a>
						</li>
						<li class="service-card">
							<a href="/optimisation-seo" class="service-link">
								<Icon icon="rocket-launch" color="var(--accent-regular)" size="2.5rem" gradient />
								<h4>Visibilité sur Google</h4>
								<p>Amélioration de votre référencement naturel (SEO) pour attirer durablement de nouveaux clients.</p>
							</a>
						</li>
						<li class="service-card">
							<a href="/formation" class="service-link">
								<Icon icon="strategy" color="var(--accent-regular)" size="2.5rem" gradient />
								<h4>Formation & transmission</h4>
								<p>Apprenez à maîtriser vos outils numériques pour monter en compétence en toute autonomie.</p>
							</a>
						</li>
						<li class="service-card">
							<Icon icon="users-three" color="var(--accent-regular)" size="2.5rem" gradient />
							<h4>Accompagnement projet</h4>
							<p>Conseil stratégique et suivi humain tout au long de la vie de votre projet digital.</p>
						</li>
					</Grid>
				</div>
			</section>

			<main class="wrapper stack gap-12 lg:gap-24">
				<section id="projets" class="section projects" style="padding-top: 1rem;">
					<div class="container stack gap-10 lg:gap-12">
						<header class="section-header stack gap-4 centered">
							<div class="badge-accent mx-auto">Réalisations</div>
							<h2>Projets réalisés</h2>
							<p class="max-w-2xl mx-auto">Une sélection de mes réalisations les plus marquantes, alliant expertise technique et approche humaine.</p>
						</header>

						<div class="gallery">
							<Grid>
								{
									projects.map((project) => (
										<li>
											<PortfolioPreview project={project} />
										</li>
									))
								}
							</Grid>
						</div>

						<div class="cta centered">
							<CallToAction href="/work/">
								Explorer toutes les réalisations
								<Icon icon="arrow-right" size="1.2em" />
							</CallToAction>
						</div>
					</div>
				</section>

			<section id="univers" class="section universe">
					<div class="container stack gap-10 lg:gap-15">
					<header class="section-header stack gap-4 centered">
						<div class="badge-accent mx-auto">Mon approche</div>
						<h2>Expertise & Valeurs</h2>
						<p class="max-w-2xl mx-auto">
							Développeur web freelance avec plus de 10 ans d'expérience, j'accompagne mes clients dans la création de solutions digitales performantes et humaines.
						</p>
					</header>
	
					<div class="gallery">
						<div class="universe-grid">
							{
								[
									{ id: 'bienveillance', label: 'Bienveillance', icon: 'heart', description: 'Une approche humaine centrée sur l’écoute et l’empathie.' },
									{ id: 'technique', label: 'Rigueur technique', icon: 'code', description: 'Un code propre, robuste et performant.' },
									{ id: 'clarté', label: 'Clarté & pédagogie', icon: 'sun', description: 'Démystifier la technique pour vous donner une vision claire.' },
									{ id: 'transmission', label: 'Transmission', icon: 'strategy', description: 'Partager le savoir pour favoriser votre autonomie.' },
									{ id: 'outillage', label: 'Technologies', icon: 'terminal', description: 'Sélection des meilleurs outils pour répondre à vos besoins.' },
									{ id: 'automatisation', label: 'Gain de temps', icon: 'magic-wand', description: 'Automatiser les tâches répétitives pour libérer votre créativité.' },
								].map((item) => (
									<div class="universe-item" data-universe-id={item.id} data-universe-data={JSON.stringify(item)}>
										<div class="universe-card-inner">
											<div class="icon-box">
												<Icon icon={item.icon as any} color="var(--accent-regular)" size="2rem" gradient />
											</div>
											<h4>{item.label}</h4>
											<p>{item.description}</p>
										</div>
									</div>
								))
							}
						</div>
					</div>
	
					<div class="cta centered">
						<button class="explore-universe-btn" id="open-universe-modal">
							<span>Explorer mon univers complet</span>
							<Icon icon="arrow-right" size="1.2em" />
						</button>
					</div>
				</div>
			</section>

				<Testimonials />
				<section id="faq" class="section faq-section">
					<div class="container stack gap-10 lg:gap-15">
							<header class="section-header stack gap-4 centered">
								<div class="badge-accent mx-auto">FAQ</div>
								<h2>Questions fréquentes</h2>
								<p class="max-w-2xl mx-auto">Tout ce qu'il faut savoir sur la création de sites web sur mesure, l'optimisation SEO et l'automatisation.</p>
							</header>
							<div class="faq-grid">
								<details class="faq-item">
								<summary>Pourquoi choisir un site web sur mesure ?</summary>
								<div class="faq-content">
									<p>Un site sur mesure s’adapte aux besoins spécifiques de votre activité. Contrairement aux templates génériques, il permet d’optimiser la performance, le référencement naturel (SEO) et l'expérience utilisateur pour une efficacité maximale.</p>
								</div>
							</details>
							<details class="faq-item">
								<summary>Comment améliorer la visibilité d'un site sur Google ?</summary>
								<div class="faq-content">
									<p>La visibilité sur Google repose sur plusieurs piliers : une optimisation SEO technique rigoureuse, un contenu pertinent répondant aux intentions de recherche, des performances de chargement élevées et une structure sémantique claire.</p>
								</div>
							</details>
							<details class="faq-item">
								<summary>Qu'est-ce que l'optimisation pour les IA (GEO) ?</summary>
								<div class="faq-content">
									<p>Le GEO (Generative Engine Optimization) consiste à structurer et optimiser votre contenu pour qu'il soit facilement compris et cité par les moteurs de recherche assistés par intelligence artificielle (comme ChatGPT, Perplexity ou Google Search Generative Experience).</p>
								</div>
							</details>
							<details class="faq-item">
								<summary>Quels sont les délais pour la création d'un site sur mesure ?</summary>
								<div class="faq-content">
									<p>Les délais varient selon la complexité du projet. En général, il faut compter entre 4 et 8 semaines pour une solution complète, incluant la conception, le développement technique et l'optimisation pour le référencement naturel.</p>
								</div>
							</details>
						</div>
					</div>
				</section>
				<section id="formation" class="section formation-premium">
					<div class="formation-bg"></div>
					<div class="formation-container stack gap-12 lg:gap-16">
						<header class="section-header stack gap-4 centered">
							<div class="badge-accent mx-auto">Formation & Accompagnement</div>
							<h2>Formation et accompagnement</h2>
							<p class="max-w-2xl mx-auto">
								Bâtissez votre projet (WordPress, SEO, Design) tout en apprenant à le maîtriser. 
								Profitez d'un financement (CPF, OPCO) via notre centre partenaire certifié AFI.
							</p>
						</header>

						<div class="formation-content-grid">
							<div class="formation-image-wrapper">
								<img
									alt="Florent Murat - Formation et transmission de savoir-faire"
									src="/assets/letItBe/eveil_et_science.jpeg" 
									loading="lazy" 
									decoding="async" 
								/>
								<div class="image-badge">
									<Icon icon="strategy" size="1.2em" />
									<span>Apprentissage concret</span>
								</div>
							</div>

							<div class="formation-features stack gap-8">
								<div class="feature-item">
									<div class="icon-circle">
										<Icon icon="rocket-launch" size="1.5rem" gradient />
									</div>
 								<div class="feature-text stack gap-1">
										<h4>Expertise de 10+ ans</h4>
										<p>Une expérience solide acquise sur plus de 50 projets web réussis.</p>
									</div>
								</div>
								<div class="feature-item">
									<div class="icon-circle">
										<Icon icon="strategy" size="1.5rem" gradient />
									</div>
									<div class="feature-text stack gap-1">
										<h4>Autonomie réelle</h4>
										<p>Ne soyez plus dépendant. Apprenez à gérer votre interface et vos contenus en toute confiance.</p>
									</div>
								</div>
								<div class="feature-item">
									<div class="icon-circle">
										<Icon icon="code" size="1.5rem" gradient />
									</div>
									<div class="feature-text stack gap-1">
										<h4>Expertise sur mesure</h4>
										<p>Le programme s'adapte à votre projet : WordPress, SEO ou structuration de données.</p>
									</div>
								</div>
								<div class="feature-item">
									<div class="icon-circle">
										<Icon icon="heart" size="1.5rem" gradient />
									</div>
									<div class="feature-text stack gap-1">
										<h4>Financement facilité</h4>
										<p>Accompagnement administratif pour vos dossiers CPF, OPCO ou Pôle Emploi.</p>
									</div>
								</div>

								<div class="cta-group">
									<CallToAction href="/formation">
										Découvrir l'offre de formation
										<Icon icon="arrow-right" size="1.2em" />
									</CallToAction>
								</div>
							</div>
						</div>
					</div>
				</section>
			</main>
		</div>
		<ContactCTA id="contact" />
	</div>
</BaseLayout>

<script>
	const openUniverseBtn = document.getElementById('open-universe-modal');
	const universeModal = document.getElementById('universe-modal') as HTMLDialogElement;

	if (openUniverseBtn && universeModal) {
		openUniverseBtn.addEventListener('click', () => {
			universeModal.showModal();
		});
	}

	const openQuotePill = document.getElementById('open-quote-pill');
	if (openQuotePill) {
		openQuotePill.addEventListener('click', (e) => {
			e.preventDefault();
			window.dispatchEvent(new CustomEvent('open-contact', {
				detail: {
					tab: 'quote',
					message: "Bonjour, je souhaiterais discuter d'un accompagnement sur mesure pour mon projet..."
				}
			}));
		});
	}
</script>

<style>
	.hero {
		display: flex;
		flex-direction: column;
		align-items: center;
		gap: 3rem;
	}

	.roles {
		display: grid;
		grid-template-columns: repeat(2, 1fr);
		gap: 0.5rem;
		width: 100%;
	}

	@media (min-width: 50em) {
		.roles {
			display: flex;
			flex-wrap: wrap;
			width: auto;
		}
	}

	.hero-ctas {
		display: flex;
		flex-wrap: wrap;
		gap: 1rem;
		justify-content: center;
		margin-top: 1rem;
	}

	.hero-image-container {
		position: relative;
		overflow: hidden;
		border-radius: var(--radius-md);
		box-shadow: var(--shadow-md);
		transition: transform var(--theme-transition-slow), box-shadow var(--theme-transition);
	}

	.hero-image-container:hover {
		transform: scale(1.02);
		box-shadow: var(--glow-accent);
	}

	.hero-image-container img {
		display: block;
		width: 100%;
		height: auto;
		aspect-ratio: 5 / 4;
		object-fit: cover;
		object-position: center;
		transition: transform var(--theme-transition-slow);
	}

	.hero-image-container:hover img {
		transform: scale(1.05);
	}

	.image-overlay {
		position: absolute;
		inset: 0;
		display: flex;
		flex-direction: column;
		justify-content: flex-end;
		padding: 2.5rem;
		background: linear-gradient(to top, rgba(0, 0, 0, 0.8) 0%, transparent 60%);
		opacity: 0;
		transition: opacity var(--theme-transition);
		color: white;
		pointer-events: none;
	}

	.hero-image-container:hover .image-overlay {
		opacity: 1;
	}

	.overlay-title {
		font-size: var(--text-lg);
		font-weight: bold;
		margin-bottom: 0.25rem;
		transform: translateY(10px);
		transition: transform var(--theme-transition-slow);
	}

	.hero-image-container:hover .overlay-title {
		transform: translateY(0);
	}

	.overlay-text {
		font-size: var(--text-sm);
		opacity: 0.9;
		line-height: 1.4;
		transform: translateY(10px);
		transition: transform var(--theme-transition-slow) 0.1s;
	}

	.hero-image-container:hover .overlay-text {
		transform: translateY(0);
	}

	.img-center {
		margin:1.5rem 0;
	}

	.img-center img {
		width: 120rem;
	}

	p.formation {
		padding: 1.2rem;
	}

	@media (min-width: 50em) {
		.hero {
			display: grid;
			grid-template-columns: 6fr 4fr;
			padding-inline: 1.5rem;
			gap: 3rem;
			align-items: center;
		}

		.roles {
			margin-top: 0.5rem;
			justify-content: flex-start;
		}

		.hero-ctas {
			justify-content: flex-start;
		}

		.hero-image-container {
			border-radius: 3rem;
		}

		.hero-image-container img {
			aspect-ratio: 3 / 4;
		}
	}

	@media (min-width: 80em) {
		.hero {
			padding-inline: 2.5rem;
			gap: 5rem;
		}

		.hero-image-container {
			border-radius: 4.5rem;
		}
	}
	@media (max-width: 50em) {
		.formation h4{
			max-width: 20ch;
			font-size: var(--text-xl);
		}

		.formation p{
			max-width: 31ch;
		}

		.formation img{
			max-width: 18rem;
 		   padding-right: 1em;;
		}
	}

	.service-card {
		display: flex;
		flex-direction: column;
		align-items: center;
		text-align: center;
		padding: 2.5rem;
		background: var(--gray-999);
		border: 1px solid var(--gray-800);
		border-radius: var(--radius-md);
		box-shadow: var(--shadow-sm);
		transition: transform var(--theme-transition), 
					box-shadow var(--theme-transition), 
					border-color var(--theme-transition);
		gap: 1.5rem;
		height: 100%;
		position: relative;
	}

	.service-link {
		text-decoration: none;
		color: inherit;
		display: flex;
		flex-direction: column;
		align-items: center;
		gap: 1.5rem;
		width: 100%;
		height: 100%;
	}

	.service-card:hover {
		transform: translateY(-8px);
		box-shadow: var(--glow-accent);
		border-color: var(--accent-regular);
	}

	.service-card:focus-within {
		outline: 3px solid var(--accent-regular);
		outline-offset: 4px;
	}

	.service-card:hover h4 {
		color: var(--accent-regular);
	}

	.service-card h4 {
		font-size: var(--text-lg);
		color: var(--gray-0);
	}

	.service-card p {
		font-size: var(--text-sm);
		color: var(--gray-300);
		line-height: 1.5;
	}

	/* Univers section specific styles */
	.universe-card {
		position: relative;
		display: flex;
		flex-direction: column;
		align-items: center;
		justify-content: center;
		padding: 2.5rem;
		background: var(--gray-999);
		border: 1px solid var(--gray-800);
		border-radius: var(--radius-md);
		transition: transform var(--theme-transition), 
					box-shadow var(--theme-transition), 
					border-color var(--theme-transition);
		gap: 1rem;
		height: 100%;
		cursor: help;
	}

	.universe-card:hover {
		transform: translateY(-5px);
		box-shadow: var(--glow-accent);
		border-color: var(--accent-regular);
	}

	.universe-card p {
		margin-top: 0.75rem;
		font-weight: 600;
		color: var(--gray-0);
		font-size: var(--text-sm);
	}

	@media (min-width: 50em) {
		.universe-card p {
			font-size: var(--text-md);
		}
	}

	.universe-tooltip {
		position: absolute;
		bottom: calc(100% + 15px);
		left: 50%;
		transform: translateX(-50%) translateY(10px);
		width: 220px;
		padding: 1rem;
		background: var(--gray-0);
		color: var(--gray-999);
		border-radius: var(--radius-sm);
		font-size: var(--text-sm);
		text-align: center;
		opacity: 0;
		visibility: hidden;
		transition: opacity 0.3s ease, transform 0.3s ease, visibility 0.3s;
		box-shadow: var(--shadow-lg);
		z-index: 10;
		pointer-events: none;
	}

	.universe-tooltip::after {
		content: '';
		position: absolute;
		top: 100%;
		left: 50%;
		transform: translateX(-50%);
		border: 8px solid transparent;
		border-top-color: var(--gray-0);
	}

	.universe-card:hover .universe-tooltip {
		opacity: 1;
		visibility: visible;
		transform: translateX(-50%) translateY(0);
	}

	.explore-universe-btn {
		display: flex;
		align-items: center;
		gap: 0.8rem;
		padding: 0.8rem 2rem;
		background: transparent;
		color: var(--accent-regular);
		border: 2px solid var(--accent-regular);
		border-radius: var(--radius-full);
		font-size: var(--text-md);
		font-weight: bold;
		cursor: pointer;
		transition: all var(--theme-transition);
		margin: 2rem auto 0;
	}

	@media (min-width: 50em) {
		.explore-universe-btn {
			padding: 1.2rem 2.5rem;
			font-size: var(--text-lg);
		}
	}

	.explore-universe-btn:hover {
		background: var(--accent-regular);
		color: white;
		box-shadow: var(--glow-accent);
		transform: translateY(-2px);
	}

	.explore-universe-btn:focus-visible {
		outline: 3px solid var(--accent-regular);
		outline-offset: 4px;
	}

	.service-card p {
		font-size: var(--text-sm);
		color: var(--gray-300);
		line-height: 1.5;
	}

	/* Universe Section Styles */
	.universe {
		padding: 3rem 0;
		position: relative;
		overflow: hidden;
	}

	.universe-grid {
		display: grid;
		gap: 1.5rem;
		grid-template-columns: repeat(auto-fit, minmax(280px, 1fr));
	}

	@media (min-width: 50em) {
		.universe-grid {
			grid-template-columns: repeat(3, 1fr);
			gap: 2rem;
		}
	}

	.universe-item {
		list-style: none;
	}

	.universe-card-inner {
		padding: 2rem;
		background: var(--gray-999_40);
		border: 1px solid var(--gray-800);
		border-radius: 1.5rem;
		height: 100%;
		display: flex;
		flex-direction: column;
		align-items: center;
		text-align: center;
		transition: all 0.4s cubic-bezier(0.16, 1, 0.3, 1);
		backdrop-filter: blur(10px);
	}

	.universe-card-inner:hover {
		transform: translateY(-6px);
		border-color: var(--accent-regular);
		background: var(--gradient-subtle);
		box-shadow: var(--shadow-md), var(--glow-accent);
	}

	.icon-box {
		margin-bottom: 1.5rem;
		padding: 1rem;
		background: var(--gray-800);
		border-radius: 1rem;
		transition: transform 0.4s ease;
	}

	.universe-card-inner:hover .icon-box {
		transform: scale(1.1) rotate(5deg);
		background: var(--accent-overlay);
	}

	.universe-card-inner h4 {
		font-size: var(--text-lg);
		color: var(--gray-0);
		margin-bottom: 1rem;
	}

	.universe-card-inner p {
		font-size: var(--text-sm);
		color: var(--gray-300);
		line-height: 1.6;
	}

	.centered {
		text-align: center;
		margin-inline: auto;
	}

	.mx-auto {
		margin-inline: auto;
	}

	.max-w-2xl {
		max-width: 65ch;
	}

	.solutions {
		margin-top: 0;
		position: relative;
		overflow: hidden;
	}

	.solutions-bg {
		position: absolute;
		top: 0;
		left: 0;
		width: 100%;
		height: 100%;
		background: radial-gradient(circle at 80% 20%, var(--accent-overlay) 0%, transparent 40%);
		z-index: -1;
		opacity: 0.4;
	}

	@media (min-width: 50em) {
		.solutions {
			margin-top: -2rem;
		}
	}

	/* ====================================================== */

	.section {
		display: grid;
		gap: 2rem;
	}

	.with-background {
		position: relative;
	}

	.with-background::before {
		--hero-bg: var(--bg-image-subtle-2);

		content: '';
		position: absolute;
		pointer-events: none;
		left: 50%;
		width: 100vw;
		aspect-ratio: calc(2.25 / var(--bg-scale));
		top: 0;
		transform: translateY(-75%) translateX(-50%);
		background:
			url('/assets/backgrounds/noise.png') top center/220px repeat,
			var(--hero-bg) center center / var(--bg-gradient-size) no-repeat,
			var(--gray-999);
		background-blend-mode: overlay, normal, normal, normal;
		mix-blend-mode: var(--bg-blend-mode);
		z-index: -1;
	}

	.with-background.bg-variant::before {
		--hero-bg: var(--bg-image-subtle-1);
	}

	.section-header p {
		font-size: var(--text-md);
		color: var(--gray-300);
		max-width: 65ch;
		margin-inline: auto;
	}

	.section-header {
		text-align: center;
	}

	@media (min-width: 50em) {
		.section-header p {
			font-size: var(--text-lg);
		}
	}

	.section-header h3 {
		font-size: var(--text-2xl);
		margin-bottom: 0.5rem;
		line-height: 1.2;
		letter-spacing: -0.01em;
		color: var(--gray-0);
	}

	.badge-accent {
		display: inline-block;
		padding: 0.25rem 1rem;
		border-radius: 99rem;
		background: var(--accent-overlay);
		color: var(--accent-regular);
		font-size: var(--text-xs);
		font-weight: 700;
		text-transform: uppercase;
		letter-spacing: 0.1em;
		border: 1px solid var(--accent-regular);
		margin-bottom: 0.5rem;
	}

	@media (min-width: 50em) {
		.section {
			grid-template-columns: repeat(4, 1fr);
			grid-template-areas: 'header header header header' 'gallery gallery gallery gallery';
			gap: 5rem;
		}

		.section.with-cta {
			grid-template-areas: 'header header header cta' 'gallery gallery gallery gallery';
		}

		.section-header {
			grid-area: header;
			justify-self: center;
			text-align: center;
		}

		.section-header h3 {
			font-size: var(--text-3xl);
		}

		.with-cta .section-header {
			justify-self: center;
			text-align: center;
		}

		.gallery {
			grid-area: gallery;
		}

		.cta {
			grid-area: cta;
			align-self: center;
			justify-self: flex-end;
		}
	}

	@media (min-width: 80em) {
		.section-header h3 {
			font-size: var(--text-4xl);
		}
	}

	/* ====================================================== */

	.mention-card {
		display: flex;
		height: 7rem;
		justify-content: center;
		align-items: center;
		text-align: center;
		border: 1px solid var(--gray-800);
		border-radius: 1.5rem;
		color: var(--gray-300);
		background: var(--gradient-subtle);
		box-shadow: var(--shadow-sm);
	}

	@media (min-width: 50em) {
		.mention-card {
			border-radius: 1.5rem;
			height: 9.5rem;
		}
	}
	.projects .gallery {
		margin-top: 0;
	}

	.projects .container {
		max-width: 85rem;
		margin-inline: auto;
		width: 100%;
		padding-inline: 1.5rem;
	}

	.projects .gallery {
		margin-top: 1rem;
	}
	
	.projects .gallery li {
		list-style: none;
	}

	.projects .container .cta {
		margin-top: 2rem;
	}

	@media (min-width: 50em) {
		.projects {
			margin-top: 4rem;
			display: block; /* Désactive le grid parent pour utiliser le container centré */
		}
		.projects .gallery {
			margin-top: 2rem;
		}
		.projects .container .cta {
			margin-top: 3rem;
		}
		.projects .container {
			padding-inline: 2.5rem;
		}
	}

	@media (min-width: 80em) {
		.projects .gallery {
			margin-top: 3rem;
		}
	}

	/* Formation Premium Styles */
	.formation-premium {
		padding-block: 5rem;
	}

	.formation-container {
		background: var(--gray-999);
		border: 1px solid var(--gray-800);
		border-radius: var(--radius-lg);
		padding: 2.5rem;
		box-shadow: var(--shadow-sm);
		overflow: hidden;
	}

	.badge-accent {
		display: inline-block;
		padding: 0.25rem 0.75rem;
		background: var(--accent-subtle-overlay);
		color: var(--accent-regular);
		border-radius: var(--radius-full);
		font-size: var(--text-sm);
		font-weight: 700;
		text-transform: uppercase;
		letter-spacing: 0.05em;
		width: fit-content;
	}

	/* --- Formation Section --- */

	.formation-premium {
		position: relative;
		overflow: hidden;
		padding-block: 5rem;
	}

	.formation-bg {
		position: absolute;
		top: 0;
		right: 0;
		width: 100%;
		height: 100%;
		background: radial-gradient(circle at 80% 50%, var(--accent-overlay) 0%, transparent 50%);
		z-index: -1;
		opacity: 0.3;
	}

	.formation-container {
		background: var(--gray-999_40);
		backdrop-filter: blur(10px);
		border: 1px solid var(--gray-800);
		border-radius: var(--radius-lg);
		padding: 2.5rem;
	}

	.formation-content-grid {
		display: grid;
		gap: 3rem;
		grid-template-columns: 1fr;
		align-items: center;
	}

	.formation-image-wrapper {
		position: relative;
		border-radius: var(--radius-md);
		overflow: hidden;
		box-shadow: var(--shadow-lg);
	}

	.formation-image-wrapper img {
		width: 100%;
		height: 100%;
		object-fit: cover;
		aspect-ratio: 4 / 3;
		transition: transform var(--theme-transition-slow);
	}

	.formation-image-wrapper:hover img {
		transform: scale(1.05);
	}

	.image-badge {
		position: absolute;
		bottom: 1.5rem;
		left: 1.5rem;
		background: var(--gray-999);
		color: var(--gray-0);
		padding: 0.75rem 1.25rem;
		border-radius: var(--radius-sm);
		display: flex;
		align-items: center;
		gap: 0.75rem;
		font-weight: 700;
		font-size: var(--text-sm);
		box-shadow: var(--shadow-md);
		z-index: 2;
	}

	.feature-item {
		display: flex;
		gap: 1.5rem;
		align-items: flex-start;
	}

	.icon-circle {
		flex-shrink: 0;
		width: 3.5rem;
		height: 3.5rem;
		background: var(--gray-800);
		border-radius: 50%;
		display: flex;
		align-items: center;
		justify-content: center;
		transition: transform var(--theme-transition);
	}

	.feature-item:hover .icon-circle {
		transform: scale(1.1) rotate(5deg);
		background: var(--accent-overlay);
	}

	.feature-text h4 {
		font-size: var(--text-lg);
		color: var(--gray-0);
	}

	.feature-text p {
		color: var(--gray-300);
		font-size: var(--text-md);
		line-height: 1.6;
	}

	.cta-group {
		margin-top: 1rem;
	}

	.faq-section {
		padding: 3rem 0;
	}

	.faq-grid {
		max-width: 1000px;
		margin: 0 auto;
		display: grid;
		gap: 1.5rem;
		grid-template-columns: 1fr;
	}

	@media (min-width: 50em) {
		.faq-grid {
			grid-template-columns: 1fr 1fr;
			align-items: start;
		}
	}

	.faq-item {
		background: var(--gray-999_40);
		border: 1px solid var(--gray-800);
		border-radius: var(--radius-md);
		padding: 1rem 1.5rem;
		cursor: pointer;
		transition: all var(--theme-transition);
	}

	.faq-item:hover {
		border-color: var(--accent-regular);
		background: var(--gray-999);
	}

	.faq-item summary {
		font-weight: 700;
		color: var(--gray-0);
		padding: 0.5rem 0;
		list-style: none;
		position: relative;
		padding-right: 2rem;
	}

	.faq-item summary::after {
		content: '+';
		position: absolute;
		right: 0;
		top: 50%;
		transform: translateY(-50%);
		color: var(--accent-regular);
		font-size: 1.5rem;
		transition: transform var(--theme-transition);
	}

	.faq-item[open] summary::after {
		transform: translateY(-50%) rotate(45deg);
	}

	.faq-content {
		padding-top: 1rem;
		color: var(--gray-300);
		line-height: 1.6;
		font-size: var(--text-md);
	}

	@media (min-width: 50em) {
		.formation-premium {
			padding-block: 8rem;
		}

		.formation-container {
			padding: 5rem;
		}

		.formation-content-grid {
			grid-template-columns: 1fr 1fr;
			gap: 5rem;
		}
	}
</style>
